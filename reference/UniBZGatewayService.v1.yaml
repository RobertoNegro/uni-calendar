openapi: 3.0.0
info:
  title: UniBZGatewayService
  version: '1.0'
  description: Gateway for obtaining courses and schedules of courses of Università degli Studi di Bolzano. Not exposed outside to the outside.
servers:
  - url: 'http://localhost:3000'
paths:
  /courses:
    get:
      summary: Get list of courses
      tags: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: ./UniversitiesGatewayServices.v1.yaml#/components/schemas/Course
      operationId: get-courses
      description: Get the list of courses of UniBZ.
  '/course/{id}':
    get:
      summary: Get course events by course ID
      tags: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: ./UniversitiesGatewayServices.v1.yaml#/components/schemas/CourseEvent
      operationId: get-course
      description: Get the schedule of a course of UniBZ.
    parameters:
      - schema:
          type: string
        name: id
        in: path
        required: true
        description: Course ID
  /info:
    get:
      summary: Get university's info
      tags: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: ./UniversitiesGatewayServices.v1.yaml#/components/schemas/UniversityCreation
      operationId: get-info
      description: Get the university's info.
components:
  schemas: {}
